import React from "react";

const Contenido = () => {
  return (
    <div
      className="Contenido"
      style={{ backgroundColor: "gray", height: "100vh", width: "100%" }}
    >
      Contenido
    </div>
  );
};

export default Contenido;
